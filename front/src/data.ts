/**
 * @author jnsorn
 */

export const questions = new Map<number, string>();
questions.set(1, "시간 약속을 잘 지켜요");
questions.set(2, "친절하고 매너가 좋아요");
questions.set(3, "응답이 빨라요");

export const QUESTION_SIZE = questions.size;

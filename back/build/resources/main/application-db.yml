---

spring:
  profiles: develop

  datasource:
    url: jdbc:mariadb://seller_lee_database:3306/sl_db
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: 1234

  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      use_sql_comments: true
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL5Dialect

  flyway:
    baseline-on-migrate: true
    baseline-version: 0


logging:
  level:
    org.hibernate.type.descriptor.sql: trace
    org.springframework.web: debug

---

spring:
  profiles: local

  datasource:
    url: jdbc:mariadb://localhost:3306/sl_db
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: 1234

  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      use_sql_comments: true
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL5Dialect

  flyway:
    baseline-on-migrate: true
    baseline-version: 0


logging:
  level:
    org.hibernate.type.descriptor.sql: trace
    org.springframework.web: debug
---

spring:
  profiles: prod

  datasource:
    url: jdbc:mariadb://192.168.1.23:3306/sl_db
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: 1234

  jpa:
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      use_sql_comments: true
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL5Dialect

  flyway:
    baseline-on-migrate: true
    baseline-version: 0


logging:
  level:
    org.hibernate.type.descriptor.sql: trace
    org.springframework.web: debug
